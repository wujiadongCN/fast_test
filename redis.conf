# Redis 基本配置
bind 0.0.0.0
port 6379

# 持久化配置
save 900 1
save 300 10
save 60 10000

# 内存管理
maxmemory 400mb
maxmemory-policy allkeys-lru

# 安全设置（生产环境建议启用）
# requirepass your_redis_password

# 日志配置
loglevel notice
logfile ""

# 其他优化
tcp-keepalive 300
timeout 0